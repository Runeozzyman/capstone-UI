version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "15000:15000"
    restart: always

  frontend:
    build: .
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
